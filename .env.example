# Copia este archivo a .env y rellena los valores.
# No subas .env al repositorio (está en .gitignore).

# Base de datos (PostgreSQL, ej. Supabase)
DATABASE_URL="postgresql://user:password@host:5432/database"

# Better Auth – URL base de la app (desarrollo: http://localhost:3000)
NEXT_PUBLIC_BETTER_AUTH_URL="http://localhost:3000"

# GitHub OAuth (crear app en https://github.com/settings/developers)
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""
